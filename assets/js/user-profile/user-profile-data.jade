div(ng-controller='userProfileDataController as updCtrl', ng-init='ctrl.deactivateUserProfileTab()')
    tabset
        tab(ng-repeat='tab in updCtrl.tabs',
            heading='{{tab.title}}',
            active='tab.active',
            disable='tab.disabled')
            br
            .page(ng-include='', src="tab.content")
